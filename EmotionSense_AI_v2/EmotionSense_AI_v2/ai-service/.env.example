# Gemini API — transcription + speaker diarization + call summary
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# INR conversion rate (used for cost display)
USD_TO_INR=84

# Call summary (uses Gemini — set to false to skip)
CALL_SUMMARY_ENABLED=true
